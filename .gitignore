# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# env
.env
.env*.local

# prisma / sqlite local (dados reais de desenvolvimento)
**/dev.db
**/dev.db-journal
**/*.db
**/*.db-journal

# planilhas de origem (dados sensíveis: CPF, valores, dados pessoais) — já
# importadas para o banco, não precisam ir para o repositório
*.xlsx
*.xls

# exportações de relatório (podem conter dados sensíveis, geradas sob demanda)
*.pdf

# typescript
*.tsbuildinfo
next-env.d.ts

# logs
npm-debug.log*
